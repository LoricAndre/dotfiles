[global]
frame_color = "#${color5}"
separator_color = frame
follow = mouse
mouse_left_click = "do_action, close_current"
corner_radius = 4

[urgency_low]
background = "#${color0}"
foreground = "#${color7}"

[urgency_normal]
background = "#${color0}"
foreground = "#${color7}"

[urgency_critical]
background = "#${color0}"
foreground = "#${color7}"
frame_color = "#${color1}"
