$DOTS_DIR/bin/dots _install hyprland hypridle hyprpaper hyprlock grimblast

if [ -v $HYPRLAND_INSTANCE_SIGNATURE ]; then
	hyprpm update
	hyprpm add https://github.com/shezdy/hyprsplit
	hyprpm enable hyprsplit
fi
