local position = "left" -- Waybar position (top|bottom|left|right)
return {
  layer = "top",        -- Waybar at top layer
  position = position,
  -- height = 30, -- Waybar height (to be removed for auto height)
  -- width = 40, -- Waybar width
  spacing = 12, -- Gaps between modules
}
