#!/usr/bin/zsh
. ~/.cache/wal/colors.sh
swaymsg reload
lule_colors

cat > ~/.config/i3status-rust/themes/lule.toml <<- EOM
idle_bg = "$background"
idle_fg = "$foreground"
info_bg = "$color8"
info_fg = "$background"
good_bg = "$color12"
good_fg = "$background"
warning_bg = "$color4"
warning_fg = "$background"
critical_bg = "$color25"
critical_fg = "$background"
separator = " "
separator_fg = "auto"
separator_bg = "auto"
alternating_tint_bg = "$background"
alternating_tint_fg = "$background"
EOM
