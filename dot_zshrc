export ZSH_CONFIG_DIR="${XDG_CONFIG_DIR:-$HOME/.config}/zsh"
if [[ -d $ZSH_CONFIG_DIR ]]; then
  for f in $ZSH_CONFIG_DIR/*; do
    . "$f"
  done
fi

eval "$(atuin init zsh)"

# add Pulumi to the PATH
export PATH=$PATH:/home/loric/.pulumi/bin
